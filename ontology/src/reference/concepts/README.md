---
summary: "Guidance for adding repo-local ontology concepts."
read_when:
  - "When introducing a new repo-local concept"
  - "When deciding whether a concept belongs locally or in a shared layer"
---

# Repo Concepts

Add repo-local concepts here only when needed.

Guideline: if a concept is useful across many repos, propose it in the company or kernel overlay instead of keeping it local here.
