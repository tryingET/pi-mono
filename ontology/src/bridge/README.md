---
summary: "Purpose of the ontology bridge layer."
read_when:
  - "When mapping ontology concepts to repo artifacts"
  - "When deciding where concept-to-code links belong"
---

# Bridge

The bridge layer links ontology concepts to repo artifacts such as code symbols, APIs, schemas, or docs.

- `mapping.yaml` maps stable concept IDs to repo-local symbols or paths.
