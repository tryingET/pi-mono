---
summary: "Manual entry point for browsing the repo ontology surface."
read_when:
  - "When navigating ontology files by hand"
  - "When deciding where a repo-local concept or mapping should live"
---

# Ontology Index (repo)

Start here when browsing manually.

- `ontology/manifest.yaml` — which layers apply
- `ontology/src/system4d.yaml` — repo-local System4D concepts
- `ontology/src/reference/concepts/` — repo-local concepts, only when needed
- `ontology/src/bridge/mapping.yaml` — concept-to-artifact mappings
- `ontology/dist/` — generated artifacts
