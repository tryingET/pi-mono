---
summary: "Tactical goals for this project."
read_when:
  - "When planning sprints/weeks"
---

# Tactical Goals

- Goal: Decide the first upstream import/sync posture for `badlogic/pi-mono`.
  - Definition of done: one repo-local decision states how upstream code enters this fork, how future syncs happen, and what evidence is required before local divergence lands.

- Goal: Convert the prior DSPY upstream issue set into a local work queue.
  - Definition of done: the abandoned upstream asks are translated into repo-local issues/tasks with an explicit first slice.

- Goal: Keep the bootstrap repo truthful.
  - Definition of done: `README.md`, `next_session_prompt.md`, and `governance/work-items.json` reflect the actual current phase rather than a generic template state.
