---
summary: "System4D: Fog (risks/assumptions/exceptions/debt) for this project."
read_when:
  - "When tracking uncertainty"
---

# System4D — Fog

## Risks
- Upstream import/sync posture is still undecided, so code work could drift if that choice is deferred too long.
- The bootstrap repo currently uses a normalized `tpl-project-repo` control plane, so future structure may need adjustment once upstream code lands.
- The DSPY backlog exists mostly in issue-tracker artifacts today; if not translated locally, intent can become fragmented again.
