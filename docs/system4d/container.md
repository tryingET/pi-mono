---
summary: "System4D: Container (boundary/constraints) for this project."
read_when:
  - "When scoping project work"
---

# System4D — Container

## Boundary
- In scope: fork rationale, upstream import/sync policy, repo-local planning, and future DSPY-oriented divergence work.
- Out of scope: treating this repo as merely an issue mirror, or importing upstream code without a documented sync posture.

## Constraints
- Keep fork intent explicit in repo-local decisions.
- Keep lane placement truthful: this is a `softwareco/fork` repo, not an `owned/` or `contrib/` repo.
- Prefer bounded slices over large speculative roadmap dumps.
